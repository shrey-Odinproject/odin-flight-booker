<%= render partial: "search_form", locals: {all_airport_options: @all_airport_options, date_options: @date_options, num_passenger_options: @num_passenger_options}  %>

<%# passenger and result both are calculated from search form  %>

<% if @result.exists? %>
    <%= render partial: "search_result_form", locals: {result: @result, passenger_result: @passenger_result} %>
<%else%>
    no flights
<%end%>
